# Доменная таксономия (section -> домен) и курируемое ядро метрик.
# ФИНАЛ: domains — Ф1/Ф2; core — Ф2 (после отбора на реальных данных, 3 фильтра).
domains: {}    # "Денежные доходы населения и их использование": income, ...
core: []       # [{metric_id: <int>, name: <str>, domain: <str>, higher_is_better: <bool>}, ...]
