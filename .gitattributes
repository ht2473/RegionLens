# Аналитическое хранилище DuckDB — бинарный файл витрины. Версионируется через Git LFS,
# чтобы приложение поднималось «из коробки», а история репозитория не разбухала при
# пересборках. Требует установленного git-lfs (`git lfs install`).
data/regionlens.duckdb filter=lfs diff=lfs merge=lfs -text
