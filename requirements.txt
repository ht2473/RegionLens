# Автор: Кузьмин Евгений Олегович (студенческий билет № 70232275).
# requirements.txt синхронизирован с pyproject.toml (группы dependencies + pipeline + backend).
# После первой установки зафиксируйте точные версии: `pip freeze > requirements.txt`
# (или `uv export --no-hashes -o requirements.txt`). Инструменты разработки (pytest/ruff/
# mypy/pre-commit) ставятся отдельно: `pip install -e ".[dev]"`.

# --- ядро (общее) ---
polars>=1.0
duckdb>=1.0
pyyaml>=6.0
structlog>=24.1

# --- backend (Django + DRF) ---
django>=5.0,<6.0
djangorestframework>=3.15
django-environ>=0.11
drf-spectacular>=0.27
psycopg[binary]>=3.1
openpyxl>=3.1
python-docx>=1.1

# --- конвейер (аналитика / ML) ---
pandas>=2.2
scikit-learn>=1.4
scipy>=1.13
statsmodels>=0.14
sktime>=0.30
shap>=0.45
mapie>=0.8
ruptures>=1.1
mlflow>=2.13
joblib>=1.4
pandera>=0.20
dvc>=3.50
