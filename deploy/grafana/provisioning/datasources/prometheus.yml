# deploy/grafana/provisioning/datasources/prometheus.yml
# Автоматическое подключение Prometheus как источника данных Grafana при старте.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
